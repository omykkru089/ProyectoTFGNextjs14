/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['gaming-cdn.com', 'drive.google.com', 'r2.fivemanage.com','youtube.com', 'imgs.search.brave.com'], // Agrega los dominios permitidos aquí
    },
  };
  
  module.exports = nextConfig;